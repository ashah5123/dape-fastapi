# DAPE - Domain-Adaptive PEFT Engine
# Zero-cost, open-source dependencies only

# Core ML/Deep Learning
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
peft>=0.7.0
safetensors>=0.4.0

# Optional optimizations (for GPU efficiency)
bitsandbytes>=0.41.0  # Optional: 8-bit quantization support
sentence-transformers>=2.2.0  # Optional: semantic similarity evaluation

# Data handling
datasets>=2.14.0
tqdm>=4.66.0

# Web framework and API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0

# Data fetching and parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
markdown>=3.5.0

# Utilities
pyyaml>=6.0.1
huggingface_hub
